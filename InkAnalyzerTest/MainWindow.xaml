<Window x:Name="InkWindow" x:Class="InkAnalyzerTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="800" Width="1200" Loaded="InkWindow_Loaded">
    <Grid Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="80*" />
            <RowDefinition Height="20*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*" />
            <ColumnDefinition Width="60*" />
            <ColumnDefinition Width="20*" />
        </Grid.ColumnDefinitions>

        <Rectangle Name="SideBarTrigger" Grid.Column="0" Grid.RowSpan="2" Visibility="Collapsed" />
        <InkCanvas Name="MainInkCanvas" Grid.Column="1" Grid.RowSpan="2" />
        <InkCanvas Name="SideInkCanvas" Grid.Column="0" Grid.RowSpan="2"  />
        <TreeView Name="AnalysisView" Grid.Column="2" Grid.Row="0" SelectedItemChanged="AnalysisView_SelectedItemChanged" />

        <StackPanel Grid.Column="2" Grid.Row="1">
            <Button Name="AnalyzeButton" Content="Analyze" Click="AnalyzeButton_Click" Height="50" />
            <CheckBox Name="BoundingBoxCheckBox" Content="View Bounding Box" Margin="10" Unchecked="BoundingBoxCheckBox_Unchecked" Checked="BoundingBoxCheckBox_Checked" />
        </StackPanel>

        <Canvas Name="OverlayCanvas" Grid.Column="1" Grid.Row="0">
            <Border Name="InkInsertionCanvasParent"  BorderBrush="Black" BorderThickness="1" Visibility="Hidden">
                <InkCanvas Name="InkInsertionCanvas" Width="300" Height="100"/>
            </Border>
        </Canvas>

        <InkCanvas Name="OverlayInkCanvas" Grid.Column="1" Grid.Row="0" Background="#00000000" 
                   Visibility="Collapsed" IsHitTestVisible="False" />
    </Grid>
</Window>
